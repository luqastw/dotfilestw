# ----------------------------------------------------------------------
# Excluir Sub-repositórios de Plugins
# (O Git não consegue rastrear um repositório Git dentro de outro)
# Adapte dependendo do seu plugin manager!
# ----------------------------------------------------------------------

# Se estiver usando o Packer:
# (Geralmente em ~/.local/share/nvim/site/pack/packer/start)
# Ou se você configurou o local dos plugins para dentro de nvim/:
/plugins/

# Se estiver usando o lazy.nvim (o caminho padrão é fora, mas confira):
/lua/lazy/

# ----------------------------------------------------------------------
# Arquivos Temporários/Cache/Histórico do Neovim
# (Estes nunca devem ser versionados)
# ----------------------------------------------------------------------
/session/
/swap/
/backup/
/view/
/undo/

# Diretório padrão para cache, histórico e dados de sessão:
/cache/
/shada/

# Caminho completo do cache para lazy.nvim (pode variar):
/data/lazy/

# Arquivos de histórico do Telescope (ou outros plugins)
# (Geralmente armazenados em .local/share/nvim/shada/main.shada ou similar)
/shada/